type Query {
  firstSchema: String!
  secondSchema(input1: String, input2: String): String!
  movieSchema: Movie
}

type Mutation {
  setMovieId(id: Int): Movie
}

type Movie {
  id: Int
  title: String
  description: String
}
