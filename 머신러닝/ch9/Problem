1번 계산을 직접 실행하지 않고 계산 그래프를 만드는 주요 장점과 단점은 무엇인가요?

장점:
    1. 나누어서 계산을 할 수 있다.(속도가 더 빠르다.)

    2. 자동으로 그레디언트를 구할 수 있다.(후진 자동미분을 통해서)

    3. 확장성이 좋다.

단점: 그래프를 만드는 코드와 계산을 하는 코드 둘로 나누어서 만들어야한다.

2. 네 같습니다.

3. 앞의 결과는 두번 실행하게 되고 뒤에 결과는 한번만 실행하게됩니다. 그래서 앞에 코드에서 앞의 결과가(변수 변함, 그래프 수정) 등의 결과가 바뀌면
달라 질 수 있습니다.

4. 아니요 실행할 수 없습니다.

5. 따로 가지게 될 것 입니다.

6. 변수는 sess.run(init)로 따로 초기화 후에 session이 종료되면 사라집니다.

7. 플레이스 홀더는 처음에 크기를 지정하여 데이터를 받을 수 있는 그릇이며 session을 실행할 때 데이터를 초기화 해줍니다.

   변수는 세션이 실행할 때가 아니라 데이터 값을 처음부터 저장할 수 있는 방식입니다.

8. 올바르게 실행되지 않는다.
   일반 변수와 상수를 통해서 값을 구할 것 같다.

9. 예 가능합니다.

10. assign을 활용해서

11. 10번 입니다.

12.